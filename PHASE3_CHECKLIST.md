# Phase 진행 체크리스트 (계속)

> `PHASE0~2_CHECKLIST.md`에서 이어지는 실행 기록.

---

## Phase 3 — judge 일치율 측정 (1차 시도: Chart)

### 범위 재정의 (사용자 확정, 2026-08-16)

md 5번 섹션의 "사람 라벨 vs judge 판정 일치율"은 Phase 2의 `human_label`
(케이스 **입력값** 타당성)과는 다른 축 — judge는 AI가 생성한 **결과물**을
채점한다. 그래서 Phase 3는 새로운 라벨(`output_quality_label`/
`output_quality_note`, `human_label`/`human_note`와 이름·용도 구분)이
필요하다는 걸 확인하고 진행.

**md 5번 섹션에 추가 기록**: "케이스 타당성 라벨(Phase 2, `human_label`)과
결과물 품질 라벨(Phase 3, `output_quality_label`)은 서로 다른 축이다.
전자는 골든 케이스의 입력/기대값이 타당한지, 후자는 그 입력으로 AI가
만든 특정 결과물이 좋은지를 본다. 둘을 같은 걸로 착각하지 말 것."

**LLM Judge 타입 체크는 6개 에이전트 중 2개뿐**(md 1번 섹션 3유형 기준):
Chart(`_check_chart_quality`)와 Triage(`_check_1e`). 나머지 4개(Schedule/
Reception/Followup/Orchestrator)는 Rule 또는 Hybrid Gate라 Phase 3
대상이 아님.

**1차 시도는 Chart만**. Triage(`_check_1e`)는 여러 턴 문진 대화가 필요한데
골든 케이스엔 보호자 첫 발화 1개뿐이라 합성 대화 작성이 별도로 필요 —
Chart 결과를 보고 나서 따로 논의하기로 함.

### 인프라 이슈 — API 키 미반영 (2026-08-16)

`docker-backend-1` 컨테이너의 `OPENAI_API_KEY`가 비어 있어 실행 불가.
원인: `backend/.env`에 키가 추가된 시점이 컨테이너 **생성 이후**라
`env_file`이 반영 안 됨(`docker restart`는 `.env`를 다시 안 읽음).
`docker compose up -d backend`로 컨테이너를 재생성해서 해결. 단,
재생성으로 이번 세션 중 `docker cp`했던 모든 파일(케이스 라벨, lint
스크립트, pytest 등)이 초기 이미지 상태로 리셋돼 전부 재동기화함(호스트
파일이 원본이라 데이터 손실은 없었음, 149 passed로 재확인).

### 비용 정보

- 실행 방식: 순차 실행(케이스 1개씩, 병렬 없음), 이상 시(생성 실패/빈
  결과/judge SKIPPED/예외) 즉시 중단하도록 스크립트 작성 —
  `backend/scripts/phase3_chart_judge_run.py`
- 실제 실행: 8케이스 전부 정상 완료(8번째 케이스에서 JSON 파싱 실패 1회 →
  내장 재시도 로직으로 자동 회복, 중단 없었음)
- 호출 수: 8케이스 × 2회(생성 1 + judge 1) = 16회
- 모델: `gpt-5.4-mini`(생성·judge 동일 — Phase 7에서 분리 예정, 이번엔
  이 한계를 인지하고도 그대로 진행하기로 사용자 확정)

### 대상 케이스 (5 PASS + 3 FAIL)

Phase 2 human_label 기준 PASS 5개(구토·식욕저하, 피부 가려움, 발작,
신장 질환 의심, 고양이 요도폐색) + FAIL 3개(고양이 호흡곤란, 아나필락시스
의심, 척추 디스크 악화) — GREEN/YELLOW/ORANGE/RED 등급과 소화기·피부·
신경·비뇨기·호흡기·알레르기 카테고리가 섞이도록 선정.

### judge_status ↔ output_quality_label 일치 매핑

| judge_status | output_quality_label | 결과                             |
| ------------ | -------------------- | -------------------------------- |
| PASS         | PASS                 | 일치                             |
| WARN         | FAIL                 | 일치                             |
| PASS         | FAIL                 | 불일치(judge가 실제 문제를 놓침) |
| WARN         | PASS                 | 불일치(judge가 과잉 지적)        |
| SKIPPED      | (무관)               | 비교 제외                        |
| (무관)       | SKIP                 | 비교 제외                        |

### ⚠ 1차 시도 설계 허점 발견 (2026-08-16)

FAIL 3개를 뽑을 때 **"등급을 직접 수정한 케이스"**(고양이 호흡곤란·
척추 디스크 악화 — ORANGE→RED로 이미 고쳐놓음)와 **"등급을 그대로 둔
경계 케이스"**(아나필락시스 의심 — ORANGE 유지)를 구분하지 않고 섞어
뽑았다. 그 결과:

- 고양이 호흡곤란·척추 디스크 악화: **이미 수정된 RED 값**을 입력으로
  ChartAgent를 호출했기 때문에, "원래 있던 입력 오류가 결과물에 문제를
  남기는지" 검증이 원천적으로 불가능한 케이스가 됨 — `output_quality_label=SKIP`
  으로 비교 제외.
- 아나필락시스 의심: 유일하게 **미수정 원본(ORANGE)** 그대로 생성돼,
  이번 시도에서 실제로 의미 있는 신호를 준 케이스. judge가 "ORANGE에
  맞게"라고 판정 근거를 댔는데, 이게 독립적 판단인지 입력을 그대로
  믿고 정당화한 것인지 구분이 안 됨 — Chart의 urgency 무검증 Known
  Issue(md 9번 섹션)가 judge 판정문에서도 그대로 재현되는 걸 실제로
  확인한 사례.

**다음 시도 개선 방향**: FAIL 케이스를 고를 때 "입력이 여전히 문제
있는 상태로 남아있는 케이스"(미수정 경계 케이스)만 따로 골라야 한다 —
이미 고친 케이스를 섞으면 검증 목적 자체가 무효화된다.

### 결과

| 케이스            | Phase 2 라벨 | judge_status | output_quality_label | 일치 여부                                  |
| ----------------- | ------------ | ------------ | -------------------- | ------------------------------------------ |
| 구토·식욕저하     | PASS         | PASS         | PASS                 | 일치                                       |
| 피부 가려움       | PASS         | PASS         | PASS                 | 일치                                       |
| 발작              | PASS         | PASS         | PASS                 | 일치                                       |
| 신장 질환 의심    | PASS         | PASS         | PASS                 | 일치                                       |
| 고양이 요도폐색   | PASS         | PASS         | PASS                 | 일치                                       |
| 고양이 호흡곤란   | FAIL(수정됨) | PASS         | SKIP                 | 비교 제외(설계 허점)                       |
| 아나필락시스 의심 | FAIL(미수정) | PASS         | PASS                 | 일치(단, judge 근거 신뢰도 낮음 — 위 참고) |
| 척추 디스크 악화  | FAIL(수정됨) | PASS         | SKIP                 | 비교 제외(설계 허점)                       |

### 최종 결과 (2026-08-16)

- **비교 가능 6개 / 8개**(고양이 호흡곤란·척추 디스크 악화는 SKIP 처리 —
  이미 수정된 값으로 생성돼 원래 검증 목적과 무관해짐)
- **일치 6/6 (100%)**

### 이번 1차 시도에서 배운 것

1. **Phase 2 라벨(케이스 타당성)과 Phase 3 라벨(결과물 품질)은 실제로
   다른 결과를 낼 수 있다** — Phase 2에서 FAIL로 표시했던 3개 중, 검증
   가능했던 1개(아나필락시스 의심)도 결과물 자체는 PASS로 나왔다. "입력
   케이스에 문제가 있다"가 "생성된 결과물도 나쁘다"로 자동으로 이어지지
   않는다는 걸 실측으로 확인.
2. **표본 설계 실수**: FAIL 케이스를 고를 때 "이미 고친 케이스"와 "안
   고치고 남겨둔 경계 케이스"를 안 가려서, 3개 중 2개가 비교 불능이
   됐다. 다음 시도는 "입력이 여전히 문제 있는 상태로 남아있는 케이스"만
   골라야 한다(위 "설계 허점" 참고).
3. **100% 일치율은 이번엔 약한 신호다** — 6개 전부 judge=PASS,
   사람=PASS로만 나왔다. judge가 "진짜 나쁜 결과물"을 WARN으로 정확히
   잡아내는지는 이번 표본에 WARN/FAIL 사례가 하나도 없어서 **전혀
   검증되지 않았다.** 다음 시도에서는 의도적으로 나쁜 결과물이 나올
   가능성이 있는 케이스(부정확한 정보, 극단적 케이스 등)를 포함해야
   judge의 변별력을 제대로 볼 수 있다.
4. **의미 있는 부가 발견**: 요도폐색 케이스에서 Chart의 LLM이 명시적
   지시 없이도 "성별 미상이라 위험도 평가 제한적"이라며 gender 정보
   부재를 스스로 인지 — Triage의 규칙 기반 엔진이 구조적으로 놓치는
   지점을 LLM 기반 체크는 우연히 잡아낼 수 있다는 사례.
   `EVAL_AGENT_REDESIGN_PLAN.md` 9번 섹션 gender 이슈 항목에 追記.
5. **judge 자기평가 편향 우려가 실제로 관찰됨**: 아나필락시스 케이스에서
   judge가 "ORANGE에 맞게"라고 판정 근거를 댔는데, 이게 독립적 판단인지
   입력을 그대로 믿고 정당화한 것인지 구분이 안 됨 — 생성 모델과 judge
   모델이 같아서(`gpt-5.4-mini`) 생기는 위험을 문서(md 5번 섹션)로만이
   아니라 실제 사례로 확인. Phase 7(judge 모델 분리)의 필요성을 뒷받침.

---

## Phase 3 — 2차 시도 (조작된 결과물로 judge 변별력 테스트, 2026-08-16)

1차 시도가 전부 PASS만 나와 judge의 변별력(나쁜 결과물을 실제로 WARN
처리하는지)을 검증 못 했다는 한계를 보완. 생성 LLM 호출 없이, 1차
시도의 실제 생성 결과를 복사해 특정 필드만 직접 수정해서 "일부러
망가뜨린" 버전 3개(A·B·C) + 대조군(원본 그대로) 2개, 총 judge 호출
5회만 사용. 스크립트: `backend/scripts/phase3_chart_judge_adversarial.py`,
결과: `ai/agents/eval_cases/chart_judge_adversarial.json`.

### 조작 설계 (`_check_chart_quality`의 4개 판정 기준 중 2개 이상 겨냥)

| #    | 베이스 케이스           | 조작 내용                                                                                 | 겨냥 기준                                                              |
| ---- | ----------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| A    | 발작 (RED)              | SOAP A만 "정상 반응, 처치 불필요"로 downplay + 허위 문장("발작은 정상 생리반응")          | 기준1(응급도 모순) + 기준4(명백한 오류) — 정밀 타격형                  |
| B    | 고양이 요도폐색 (RED)   | SOAP S·A·감별진단 전부 완전히 무관한 케이스(피부/안구)로 교체                             | 기준1+2+3 동시 위반 — 총체적 환각 시나리오형(의도적으로 사용자가 승인) |
| C    | 피부 가려움 (GREEN)     | SOAP A만 "생명 위협/응급수술/24시간 내 사망"으로 극단적 과장(반대 방향 — 경증을 중증으로) | 기준1(반대 방향 모순) + 기준4                                          |
| 대조 | 구토·식욕저하 (YELLOW)  | 원본 그대로                                                                               | 재확인용                                                               |
| 대조 | 신장 질환 의심 (YELLOW) | 원본 그대로                                                                               | 재확인용                                                               |

### 결과: **5/5 정확** — 조작 3개 전부 WARN, 대조군 2개 전부 PASS

judge_detail도 정확히 조작 지점을 짚어냄:

- A: "발작을 단순 떨림/정상 생리 반응으로 축소... RED 트리아지와 명백히 모순"
- B: "SOAP A가 알레르기성 피부염/결막염... 주요 증상과 감별이 전혀 맞지 않는 명백한 모순"
- C: "패혈증·24시간 내 사망·응급수술... 응급도와 크게 모순... 확진으로 단정한 점도 부적절"

### 1차 + 2차 종합 결론

- **1차(정상 케이스 중심)**: 비교 가능 6/6 전부 일치했지만 판별력 없는 신호(WARN 사례 부재)였음.
- **2차(조작 케이스 포함)**: 판별력 있는 신호 — judge가 최소 이번 5개 표본에서는 정밀 타격형(A·C)과 총체적 붕괴형(B) 양쪽 다 정확히 잡아냄.
- **표본이 여전히 작다**(총 13개 판정: 1차 8 + 2차 5)는 점, 그리고 judge 모델이 생성 모델과 동일(`gpt-5.4-mini`)해서 자기평가 편향 위험이 구조적으로 남아있다는 점(1차의 아나필락시스 사례에서 시사)은 유효한 한계로 유지.
- Phase 4(판정 프롬프트 보정)로 넘어갈지, Chart 표본을 더 늘릴지, Triage `_check_1e`로 확장할지는 사용자 결정 필요.

### 다음에 할 일 (미정, 별도 논의 필요)

- Chart 2차 시도: "안 고친 경계 케이스"만 골라 재실행 + 의도적으로
  문제 있는 결과물이 나올 만한 케이스 추가
- Triage `_check_1e` 검증: 합성 멀티턴 대화 작성 필요(비용·작업량 더 큼)
- 인프라: `backend/.env` 변경 후 `docker compose up -d`로 재생성해야
  반영된다는 걸 팀 문서(`DEV_DOCKER.md` 등)에 남길지 검토

---

## Phase 3 — Triage `_check_1e` 검증 (1차 시도, 2026-08-18)

### 방식

Chart와 달리 Triage는 멀티턴 문진 대화가 필요한데 골든 케이스엔 보호자
첫 발화 1개뿐이라, 실제 `TriageAgent`를 턴 단위로 구동하는 드라이버
스크립트(`backend/scripts/phase3_triage_turn.py`)를 만들어 사람이 매
턴 봇의 실제 응답을 보고 다음 보호자 발화를 직접 정해서 넘기는 방식으로
진행(대사를 미리 다 써놓지 않음). 대화 완성 후
`backend/scripts/phase3_triage_judge_run.py`로 `_check_1e` 판정.

- 2케이스 × 3턴 = 턴 실행 6회 + judge 2회
- 케이스: 심한 피부 가려움(강아지, YELLOW), 다음다뇨(고양이, ORANGE)
- 상태 저장: 컨테이너 내 `/tmp/phase3/case1_skin.json`,
  `/tmp/phase3/case2_pud.json`(턴마다 덮어씀 — 중간 턴 스냅샷은 남지 않음)

### 사전 확인: RAG 시드 데이터 없음

진행 전 `triage_rag_documents` 테이블을 직접 확인 — 0건. 즉 이번 1차
시도는 RAG 유사사례 검색이 항상 "유사사례 없음"으로 나오는 상태에서
진행됐고, `suspected_conditions`가 비어 있는 건 오류가 아니라 시드
데이터 부재로 인한 예상된 동작으로 확인하고 진행.

### 결과

| 케이스                    | judge_status | 평균 점수 | 약한 항목                |
| ------------------------- | ------------ | --------- | ------------------------ |
| 심한 피부 가려움 (YELLOW) | INFO         | 7.5/10    | structuring_quality(6.0) |
| 다음다뇨 (ORANGE)         | PASS         | 8.0/10    | (전 항목 7점 이상)       |

결과 파일: `ai/agents/eval_cases/triage_judge_calibration.json`(아직
`output_quality_label`/`output_quality_note`는 미기입 — 사용자 판정
대기 중).

### RAG 시드 데이터 적재 (2026-08-18)

RAG 시드 데이터(632MB, 개인정보 없는 정적 참고자료)를 팀 공유
드라이브에서 가져와 적재함 — 팀 DB/AWS 계정은 안 쓰지만 이 정적 데이터
자산은 예외로 사용.

- 파일: `backend/data/triage/triage_rag_export.jsonl.gz`(gzip 152.6MB,
  `backend/data`가 컨테이너에 `:ro`로 라이브 바인드 마운트돼 있어 별도
  `docker cp` 불필요)
- 로더: `backend/scripts/load_triage_rag.py` — 덤프에 임베딩이 이미
  계산되어 있어 적재 자체에 추가 OpenAI 호출 없음
- 결과: `triage_rag_documents` 0건 → **19,206건(5개 진료과)** 적재 확인
  (적재 전/후 두 번 다 SQL로 직접 카운트해 확인)

### RAG 있는 상태로 재생성 비교 (2026-08-18)

같은 두 케이스를 보호자 발화는 그대로 재사용하고 RAG가 채워진 상태로
다시 생성. 스크립트: `backend/scripts/phase3_triage_judge_run_rag.py`,
결과: `ai/agents/eval_cases/triage_judge_calibration_rag.json`.

**실행 중 발견한 실수**: 1차 재생성 시도(Case 1)는 컨테이너에
`PYTHONPATH=/app`만 설정해 `_run_rag()` 내부의 `app.*` 임포트가 실패
(`No module named 'app'`) — fail-open이라 봇 응답 자체는 정상 진행됐지만
RAG가 실제로 동작하지 않은 무효 시도였음. `PYTHONPATH=/app:/app/backend`로
Case 1을 처음부터 재실행해서 바로잡음. 그 결과 실제 호출 수는 사전
견적(24회)을 넘어섬(폐기분 포함 약 34회) — 순전히 환경변수 설정 실수.

**suspected_conditions 비교** (RAG 시드 적재가 실제로 동작하는지에
대한 직접 증거):

| 케이스 | RAG 없음(1차 시도) | RAG 있음(재생성) |
|---|---|---|
| 심한 피부 가려움 | `[]` | `["피부염"]` |
| 다음다뇨 | `[]` | `["방광염", "요도염", "방광 결석", "신장 질환"]` |

RAG 시드 적재 후 유사사례 기반 의심 질환이 실제로 채워지는 것을
확인함 — RAG 적재 자체의 성공을 보여주는 근거로 기록.

**judge 결과는 순수 비교가 아님**: 두 케이스 다 이번엔 `judge_status=PASS`
(Case 1은 원래 `INFO`였는데 structuring_quality 6.0→7.0으로 상승).
다만 이번 재생성은 최종 턴만 바꾼 게 아니라 **대화 전체(1~3턴)를
처음부터 다시 생성**했고, 그 과정에서 봇의 중간 질문 문구도 달라졌고
Case 2는 urgency까지 ORANGE→YELLOW로 바뀜(추출/등급판정 자체의
비결정성) — 즉 RAG 유무 외에 다른 변수가 함께 바뀌었다. **"RAG가
judge 품질을 개선했다"는 결론은 이번 시도로 내릴 수 없다.**
RAG 시드 적재 확인은 완료됐지만, judge 품질에 대한 순수 비교는 이번
시도로 안 됨. 재시도하려면 보호자 발화뿐 아니라 봇 질문까지
고정하거나(스크립트로 대화 재생), temperature=0으로 낮춰서 비결정성을
줄여야 함.

### temperature 조사 (2026-08-18, RAG 비교는 보류)

`_triage_turn`의 4개 콜 중 추출 콜·안전판정 콜은 `call_llm_json` 기본값(0)이라
**이미 temperature=0**. 질문생성 콜(0.75)·의심질환생성 콜(0.5)만 호출부
리터럴로 박혀 있고 함수 인자·환경변수로 노출돼 있지 않음. 질문생성의
0.75는 주석("표현 다양성 ↑, 똑같은 인사 반복 완화")으로 봤을 때 의도된
값이라, eval 재현성을 위해 프로덕션 코드의 이 값을 그대로 낮추는 건
범위 밖 — 실사용자 대화 품질에 영향을 주는 변경이라 사용자 승인 없이
진행하지 않음. RAG 순수 비교는 보류하고, 대신 judge(`_check_1e`) 자체의
정확도 검증으로 우선순위 전환.

### Triage `_check_1e` 1차 검증 결과 (2026-08-18)

RAG 적용 재생성 케이스 2개(`triage_judge_calibration_rag.json`)에
사람이 직접 `output_quality_label`을 매겨 judge 판정과 비교.

| 케이스 | judge_status | 사람 output_quality_label | 일치 여부 |
|---|---|---|---|
| 심한 피부 가려움 (YELLOW) | PASS | PASS | 일치 |
| 다음다뇨 (YELLOW) | PASS | PASS | 일치 |

**비교 가능 2개 / 2개, 일치 2/2(100%)** — 단 Chart 1차 시도(6/6 일치)와
마찬가지로 표본이 매우 작고, 이번 2개 다 judge=PASS/사람=PASS로만 나와
판별력(진짜 나쁜 대화를 WARN/INFO로 정확히 잡아내는지)은 검증되지
않았다는 한계는 동일하게 적용됨.

**판단 과정에서 정정된 것**: 처음엔 "케이스 2에서 judge가 심각도 확인
부족을 지적한 게 urgency 저하(ORANGE→YELLOW)와 연결된 통찰력 있는
판정"이라고 볼 뻔했으나, 다시 검토해 이 연결은 근거가 없다고 정정함.
`_check_1e`는 대화 품질(질문을 잘 했는지)만 채점하고, urgency는 완전히
별개의 추출 콜/규칙 엔진이 계산하는 값이라 judge가 urgency 문제를
"짚어낸" 게 아니라 애초에 judge의 검증 범위 밖인 것을 같은 케이스에서
우연히 같이 관찰한 것뿐이었음. 두 케이스의 `output_quality_note`에는
"대화 품질 평가로서 타당함(urgency 계산과는 무관한 별개 검증 범위)"로
정정해서 남김. 이 재확인은 `EVAL_AGENT_REDESIGN_PLAN.md` 9번 섹션의
Chart urgency 무검증 Known Issue에도 Triage 사례로 追記함 — LLM Judge
타입 체크(Chart·Triage 공통)는 urgency 값 자체를 검증하지 않는다는
설계 공백이 두 에이전트에서 동일하게 재확인된 것.

### Triage 조작 케이스 시도 (2026-08-18)

1차 시도(2케이스 다 judge=PASS/사람=PASS)로는 판별력이 검증되지
않아, Chart 2차 시도와 같은 방식으로 특정 기준만 겨냥해 망가뜨린
대화 2개로 재시도. 생성 LLM 호출 없이 대화 텍스트를 직접 작성해
`_check_1e`에 바로 투입(judge 호출 2회만). 스크립트:
`backend/scripts/phase3_triage_judge_adversarial.py`, 결과:
`ai/agents/eval_cases/triage_judge_adversarial.json`.

| 조작 | 조작 내용 | 겨냥 기준 | completeness | question_efficiency | response_consistency | structuring_quality | judge_status |
|---|---|---|---|---|---|---|---|
| A | 후속 질문 없이 1턴 만에 종료(부위·기간·심각도 전부 미확인) | completeness(명확한 실패, 하한선 테스트) | **3.0** | 8.0 | 7.0 | **2.0** | INFO |
| B | 이미 답변받은 발병 시기를 다시 질문(구토·배변 등 새 정보 미확인) | question_efficiency(미묘한 비효율, 정밀 테스트) | **4.0** | **3.0** | **2.0** | **3.0** | INFO |

**FAIL 케이스 recall: 2/2** — 두 조작 다 judge가 INFO(이 체크의
"정상 아님" 신호)로 정확히 잡아냄.

**예측했던 부수 효과 확인**:
- A의 question_efficiency 하락 예측 → **빗나감**(8.0, 통과선 유지 —
  질문을 안 한 것 자체를 "비효율"로 보진 않은 것으로 보임)
- B의 completeness 하락 예측 → **적중**(4.0 — 새 정보를 못 모은 게
  completeness에도 반영됨)

**예측 못 한 관찰 — 교차 오염**: 두 조작 다 겨냥한 기준 하나만
깨끗하게 떨어진 게 아니라 각각 3개 기준씩 낮게 나옴. A는
completeness 외 structuring_quality도 크게 하락(정보 자체가 적어
정리할 내용도 없어진 부수 효과로 추정), B는 question_efficiency 외
response_consistency도 크게 하락(이미 답한 질문을 다시 묻는 것을
judge가 "대화 흐름과 안 맞음"으로도 해석한 것으로 추정). Chart의
조작 A/B/C는 겨냥한 기준만 정밀하게 떨어뜨릴 수 있었던 것과 대비되는
지점 — `EVAL_AGENT_REDESIGN_PLAN.md` 9번 섹션에 추가 기록.

### Triage Phase 3 완료 조건 충족 확인

md 13번 섹션 Phase 3 완료 조건("일치율 수치 산출 + FAIL 케이스 recall
별도 산출") 중 Triage에 비어 있던 FAIL recall이 이번 조작 케이스
시도(2/2)로 채워짐. Chart(1차 6/6 + 2차 5/5)와 Triage(1차 2/2 + 조작
2/2) 둘 다 일치율 산출 + FAIL recall 산출 조건을 충족 — **Triage
`_check_1e`에 대해서도 Phase 3 완료 조건이 채워진 것으로 판단.**
